[project]
name = "on-faultization-rubik-sim"
version = "0.1.0"
description = "Exhaustive faultization of the pocket cube: local-correctness voting, repair channels, deletion, structural damage, chimeric targets, and retargeting over all 3,674,160 states"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]
